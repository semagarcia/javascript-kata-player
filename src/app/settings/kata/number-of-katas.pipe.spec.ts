import { NumberOfKatasPipe } from './number-of-katas.pipe';

describe('NumberOfKatasPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberOfKatasPipe();
    expect(pipe).toBeTruthy();
  });
});
